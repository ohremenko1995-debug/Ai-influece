# The dashboard bundle is committed so `pip install identity-lock[api]` gives a
# working `identitylock serve` without a Node toolchain. It is build output:
# never edit it by hand, and do not let it clutter reviews.
src/identitylock/api/static/** linguist-generated=true -diff
web/pnpm-lock.yaml linguist-generated=true -diff

# README figures are emitted by scripts/render_readme_charts.py from real run
# data (`make charts`). Regenerate them; never hand-edit them.
docs/assets/*.svg linguist-generated=true -diff
